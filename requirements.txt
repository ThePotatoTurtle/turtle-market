# Direct dependencies only - pip resolves the rest.
# Minimum bounds (not exact pins) so wheel-available versions
# are chosen on any supported Python (3.11+).
discord.py>=2.5
aiosqlite>=0.21
python-dotenv>=1.1
matplotlib>=3.9
scipy>=1.14

# Dev / testing
pytest>=8.3
pytest-asyncio>=0.26
